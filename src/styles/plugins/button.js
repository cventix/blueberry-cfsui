export default ({ addComponents: any }) => {
  addComponents({
    '.btn': {
        padding: '.5rem 1rem',
        borderRadius: '.25rem',
        fontWeight: '600',
        backgroundColor: 'red',
    }
  })
}
